{
  "main": "dist/app.js",
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "purescript": "^0.13.6",
    "spago": "^0.15.2"
  },
  "dependencies": {
    "xhr2": "^0.2.0"
  },
  "scripts": {
    "build": "spago bundle-app --main Main --to dist/app.js",
    "serve": "parcel serve assets/index.html -o index--parcelified.html --open",
    "start": "yarn serve"    
  }
}
